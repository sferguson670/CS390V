>(define count
	(lambda (ls)
		(if (null? ls)
		0
		(+ (count (cdr ls)) 1))))

>(define lisp-cdr
	(lambda (x)
	(if (null? x)
		'()
		(cdr x))))

> (define vecadd
    (lambda (a b)
      (if (null? a)
          b
          (cons (+ (car a) (car b)) (vecadd (cdr a) (cdr b ))))))
